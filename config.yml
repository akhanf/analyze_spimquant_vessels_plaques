datasets:
  late:
    participant_tsv: /nfs/trident3/lightsheet/prado/mouse_app_lecanemab_batch2/bids/participants.tsv
    spimquant_dir: /nfs/trident3/lightsheet/prado/mouse_app_lecanemab_batch2/derivatives/spimquant-v0.6.0rc2_84a605e_ozx
  early:
    participant_tsv: /nfs/trident3/lightsheet/prado/mouse_app_lecanemab_batch3/bids/participants.tsv
    spimquant_dir: /nfs/trident3/lightsheet/prado/mouse_app_lecanemab_batch3/derivatives/spimquant-v0.6.0rc2_84a605e_ozx

template_dir: /nfs/trident3/lightsheet/prado/mouse_app_lecanemab_batch3/derivatives/spimquant-v0.6.0rc2_84a605e_ozx/tpl-ABAv3
segs:
  - roi82
  - roi198
  - all

# ── Plot configuration ────────────────────────────────────────────────────────
# Seaborn-style factor definitions.  Each named factor specifies:
#   column  : the column name in the parquet data
#   order   : ordered list of category values (first = reference / control)
#   palette : mapping from category value to hex colour
#
# primary_metric     : column used for the main statistical analysis
#                      (fold-change, regional t-tests, ANOVA, etc.)
# volume_threshold_ml: plaques larger than this value are filtered out as
#                      artefacts before any analysis or plotting.
plot_config:
  factors:
    treatment:
      column: treatment
      order: [PBS, Lecanemab]
      palette:
        PBS: "#4C72B0"
        Lecanemab: "#DD8452"
    genotype:
      column: genotype
      order: [ApoE3, ApoE4]
      palette:
        ApoE3: "#55A868"
        ApoE4: "#C44E52"
    sex:
      column: sex
      order: [M, F]
      palette:
        M: "#8172B2"
        F: "#CCB974"
  primary_metric: plaque_density
  volume_threshold_ml: 1.0e-4

